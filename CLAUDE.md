# Claude Code

Read `AGENTS.md` first — it and the docs it points to in `/docs` are the
source of truth for this repo. Load only the docs relevant to the current
task, not the whole `/docs` directory.

If a change alters architecture or product behavior, update the
corresponding doc in the same change.
